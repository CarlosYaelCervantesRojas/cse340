<% if (title) { %>
    <h1><%= title %></h1>
<% } else {
    res.redirec("/")
}%>

<%- messages() %>

<% if (errors) { %>
    <ul class="notice">
    <% errors.array().forEach(error => { %>
        <li>&#9888; <%= error.msg %></li>
    <%}) %>
    </ul>
<% } %>

<p>You're logged in</p>